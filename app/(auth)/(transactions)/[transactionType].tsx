import React from 'react';
import { View, Text } from 'react-native';

import TransactionPage from '@/src/pages/transaction/Transaction';

const Transaction: React.FC = () => {
  return <TransactionPage />;
};

export default Transaction;
